# 리팩토링 분석 및 개선 계획

> **Date**: 2025-11-29  
> **Project**: SumUp  
> **Status**: 분석 완료, 진행 중

코드베이스 분석을 통해 발견된 공통 로직, 상태 관리 개선점, 코드 스플리팅 기회를 정리한 문서입니다.

---

## 1. 공통 로직 분석

### 1.1 반복되는 패턴

#### ✅ 통계 카드 컴포넌트
**현재 상태**: 여러 페이지에서 동일한 패턴으로 통계 카드를 렌더링
- `issues/page.tsx`: 전체, 할 일, 진행 중, 완료 통계
- `projects/page.tsx`: 전체, 활성, 즐겨찾기 통계
- `projects/[id]/page.tsx`: 전체 이슈, 열린 이슈, 진행 중, 완료 통계

**개선 방안**: `StatCard` 공통 컴포넌트 추출

```tsx
// src/components/common/StatCard.tsx
interface StatCardProps {
  label: string;
  value: number | string;
  icon?: string;
  trend?: { value: number; isPositive: boolean };
  onClick?: () => void;
}
```

**우선순위**: P0 (필수)

---

#### ✅ 필터/검색 UI 패턴
**현재 상태**: 
- `issues/page.tsx`: 검색 Input + 상태 Select + 우선순위 Select
- `projects/page.tsx`: 검색 Input + 아카이브 체크박스

**개선 방안**: `FilterBar` 공통 컴포넌트 추출

```tsx
// src/components/common/FilterBar.tsx
interface FilterBarProps {
  searchPlaceholder?: string;
  searchValue: string;
  onSearchChange: (value: string) => void;
  filters?: Array<{
    type: 'select' | 'checkbox' | 'date';
    label: string;
    value: any;
    options?: Array<{ value: any; label: string }>;
    onChange: (value: any) => void;
  }>;
  onReset?: () => void;
}
```

**우선순위**: P1 (권장)

---

#### ✅ 데이터 Fetching 패턴
**현재 상태**: 
- `useIssue`, `useIssues`, `useProject`, `useProjects` 훅들이 모두 비슷한 패턴
  - `isLoading`, `error`, `data` 상태 관리
  - `refetch` 함수
  - 에러 처리 로직

**개선 방안**: 
1. 공통 데이터 fetching 훅 추상화 (`useDataFetch`)
2. React Query 도입 검토 (향후)

**우선순위**: P2 (선택)

---

#### ✅ 페이지 헤더 액션 패턴
**현재 상태**: 
- 여러 페이지에서 "뒤로가기 + 액션 버튼들" 패턴 반복
- `issues/[id]/page.tsx`: 뒤로가기 + 수정 + 상태 변경
- `projects/[id]/page.tsx`: 뒤로가기 + 수정 + 새 이슈

**개선 방안**: `PageHeader` 공통 컴포넌트 추출

```tsx
// src/components/common/PageHeader.tsx
interface PageHeaderProps {
  title: string;
  description?: string;
  backHref?: string;
  backLabel?: string;
  actions?: Array<{
    label: string;
    onClick: () => void;
    variant?: 'primary' | 'outline' | 'ghost';
    icon?: string;
  }>;
}
```

**우선순위**: P1 (권장)

---

### 1.2 공통 유틸리티 함수

#### ✅ 이미 추출된 유틸리티
- `lib/utils/issue-utils.ts`: `filterIssues`, `sortIssues`, `calculateIssueStats`
- `lib/utils/validation.ts`: 유효성 검증 함수들
- `lib/utils/toast.ts`: 토스트 메시지 헬퍼

#### 🔄 추가 추출 가능한 유틸리티
- 날짜 포맷팅 함수 (여러 곳에서 반복)
- 프로젝트 필터링/정렬 함수 (이슈와 유사한 패턴)

---

## 2. Zustand 스토어 분석

### 2.1 현재 스토어 구조

#### ✅ 기존 스토어 (4개)

1. **auth-store.ts**
   - 인증 상태 관리
   - 사용자 정보, 토큰
   - 로그인/로그아웃 액션
   - **Persist**: ✅ (localStorage)

2. **issue-filter-store.ts**
   - 이슈 필터 상태 관리
   - 검색어, 상태, 우선순위, 정렬
   - **Persist**: ❌

3. **preferences-store.ts**
   - 사용자 설정 관리
   - 테마, 언어, 폰트 스케일, 접근성 설정
   - **Persist**: ✅ (localStorage)

4. **ui-store.ts**
   - UI 상태 관리
   - 사이드바 접기/펼치기
   - **Persist**: ❌

---

### 2.2 개선 제안

#### 🔄 프로젝트 필터 스토어 추가
**이유**: 
- `projects/page.tsx`에서 로컬 상태로 관리 중 (`useState`)
- 이슈 필터와 유사한 패턴
- 여러 페이지에서 공유 가능

**구현**:
```tsx
// src/store/project-filter-store.ts
interface ProjectFilterState {
  searchQuery: string;
  teamIdFilter: string | null;
  showArchived: boolean;
  sortBy: "name" | "created" | "updated";
  sortOrder: "asc" | "desc";
  // ... actions
}
```

**우선순위**: P1 (권장)

---

#### 🔄 이슈 필터 스토어 Persist 추가
**이유**: 사용자가 설정한 필터를 유지하면 UX 향상

**구현**: `persist` 미들웨어 추가

**우선순위**: P2 (선택)

---

#### 🔄 통합 필터 스토어 고려
**이유**: 이슈 필터와 프로젝트 필터가 유사한 구조

**대안**: 
- 각각 유지 (현재 구조 유지)
- 통합 스토어 생성 (`filter-store.ts`)

**우선순위**: P2 (선택, 향후 검토)

---

## 3. 코드 스플리팅 분석

### 3.1 현재 상태

#### ❌ 코드 스플리팅 미적용
- 모든 컴포넌트가 정적 import
- 초기 번들 크기 최적화 필요

---

### 3.2 스플리팅 대상

#### ✅ 우선순위 높음 (P0)

1. **KanbanBoard 컴포넌트**
   - `@dnd-kit` 라이브러리 포함 (용량 큼)
   - 칸반 페이지에서만 사용
   - **대상**: `app/kanban/page.tsx`

```tsx
// app/kanban/page.tsx
const KanbanBoard = dynamic(() => import("@/components/kanban/KanbanBoard"), {
  ssr: false,
  loading: () => <LoadingState message="칸반 보드를 불러오는 중..." />
});
```

2. **Chart 컴포넌트들**
   - `LineChart`, `PieChart`
   - 대시보드에서만 사용 가능
   - 차트 라이브러리 용량 큼
   - **대상**: `components/charts/*`

```tsx
// app/page.tsx (대시보드)
const LineChart = dynamic(() => import("@/components/charts/LineChart"), {
  ssr: false
});
const PieChart = dynamic(() => import("@/components/charts/PieChart"), {
  ssr: false
});
```

---

#### ✅ 우선순위 중간 (P1)

3. **폼 컴포넌트들**
   - `IssueForm`, `ProjectForm` (향후 개발)
   - 모달 내에서만 사용
   - **대상**: `components/forms/*`

4. **상세 페이지 컴포넌트**
   - `CommentList`, `CommentForm`
   - 상세 페이지에서만 사용
   - **대상**: `components/issue/CommentList.tsx`, `CommentForm.tsx`

---

#### ✅ 우선순위 낮음 (P2)

5. **설정 페이지 컴포넌트**
   - 설정 페이지에서만 사용
   - **대상**: `app/settings/page.tsx` 내부 컴포넌트

---

### 3.3 스플리팅 가이드

#### Next.js Dynamic Import 사용

```tsx
import dynamic from "next/dynamic";

// 기본 사용
const Component = dynamic(() => import("./Component"));

// 로딩 상태 포함
const Component = dynamic(() => import("./Component"), {
  loading: () => <LoadingState />
});

// SSR 비활성화 (클라이언트 전용)
const Component = dynamic(() => import("./Component"), {
  ssr: false
});
```

---

## 4. 구현 계획

### Phase 1: 공통 컴포넌트 추출 (우선순위 P0)

1. ✅ `StatCard` 컴포넌트 생성
2. ✅ `FilterBar` 컴포넌트 생성 (선택)
3. ✅ 기존 페이지에 적용

**예상 시간**: 2-3시간

---

### Phase 2: Zustand 스토어 개선 (우선순위 P1)

1. ✅ `project-filter-store.ts` 생성
2. ✅ `projects/page.tsx`에 적용
3. 🔄 이슈 필터 스토어 Persist 추가 (선택)

**예상 시간**: 1-2시간

---

### Phase 3: 코드 스플리팅 적용 (우선순위 P0-P1)

1. ✅ KanbanBoard 동적 import
2. ✅ Chart 컴포넌트 동적 import
3. ✅ 폼 컴포넌트 동적 import (향후)

**예상 시간**: 1시간

---

## 5. 파일 구조 제안

```
src/
├── components/
│   ├── common/
│   │   ├── StatCard.tsx          # 새로 생성
│   │   ├── FilterBar.tsx          # 새로 생성
│   │   ├── PageHeader.tsx         # 새로 생성 (선택)
│   │   ├── LoadingState.tsx      # 기존
│   │   ├── ErrorState.tsx        # 기존
│   │   └── EmptyState.tsx        # 기존
│   └── ...
├── store/
│   ├── auth-store.ts             # 기존
│   ├── issue-filter-store.ts     # 기존
│   ├── project-filter-store.ts   # 새로 생성
│   ├── preferences-store.ts      # 기존
│   └── ui-store.ts              # 기존
└── ...
```

---

## 6. 예상 효과

### 번들 크기 감소
- KanbanBoard 스플리팅: ~50-100KB 감소
- Chart 컴포넌트 스플리팅: ~30-50KB 감소

### 코드 재사용성 향상
- 통계 카드: 3곳 → 1개 컴포넌트
- 필터 UI: 2곳 → 1개 컴포넌트

### 유지보수성 향상
- 공통 로직 중앙화
- 일관된 UI/UX

---

## 7. 레퍼런스 프로젝트 참고 사항

### 7.1 PaysByPays Dashboard (`D:\dev\dashboard`)

레퍼런스 프로젝트에서 검증된 패턴들을 참고하여 개선 방안을 수립했습니다.

#### 리팩토링 결과 (레퍼런스 프로젝트)
- **평균 코드 감소율**: 약 60%
- **재사용 컴포넌트**: 45개 생성
- **커스텀 훅**: 12개 생성
- **페이지별 리팩토링 결과**:
  - `transactions`: 700+ → 293줄 (~58% 감소)
  - `dashboard`: 574 → 207줄 (~64% 감소)
  - `settings`: 503 → 70줄 (~86% 감소)

#### 참고할 패턴

1. **필터 스토어 통합**
   - 레퍼런스: `src/store/filter-store.ts`
   - 거래 필터, 가맹점 필터를 하나의 스토어로 통합 관리
   - SumUp 적용: 이슈 필터와 프로젝트 필터 통합 검토

2. **섹션 헤더 패턴**
   - 레퍼런스: `src/components/sections/SectionHeaderBlock.tsx`
   - 일관된 섹션 헤더 UI 제공
   - SumUp 적용: `PageHeader` 컴포넌트 개발 시 참고

3. **커스텀 훅 패턴**
   - 레퍼런스: `useTransactionTable`, `useTransactionDrawer`
   - 복잡한 로직을 훅으로 캡슐화
   - SumUp 적용: 테이블 필터링/정렬 로직을 훅으로 추출 검토

4. **포맷팅 통일**
   - 레퍼런스: `useDisplayFormat` 훅
   - 모든 포맷팅을 한 곳에서 관리
   - SumUp 적용: 날짜/통화 포맷팅 통일 검토

---

## 8. 리팩토링 가이드라인

### 8.1 리팩토링 원칙

#### 기본 원칙
1. **점진적 개선**: 한 번에 모든 것을 바꾸지 않고 단계적으로 개선
2. **기능 유지**: 리팩토링 중에도 기존 기능이 정상 작동해야 함
3. **테스트 기반**: 가능한 경우 테스트를 작성한 후 리팩토링
4. **명확한 목적**: 각 리팩토링의 목적과 이유를 명확히 함
5. **문서화**: 리팩토링 내용을 문서화하여 팀과 공유

#### 리팩토링 시기
- 새로운 기능 추가 전
- 버그 수정 시
- 코드 리뷰 중 발견된 문제
- 성능 이슈 발생 시
- 기술 부채 정리 시

---

### 8.2 코드 구조 리팩토링 체크리스트

#### 컴포넌트 리팩토링
- [ ] 컴포넌트가 단일 책임을 가지는가?
- [ ] Props 타입이 명확하게 정의되어 있는가?
- [ ] 컴포넌트가 300줄 이하인가?
- [ ] 재사용 가능한 로직이 훅으로 분리되어 있는가?
- [ ] 스타일링이 일관성 있게 적용되어 있는가?

#### 훅 리팩토링
- [ ] 훅이 단일 책임을 가지는가?
- [ ] 에러 처리가 일관성 있게 구현되어 있는가?
- [ ] 로딩 상태가 적절히 관리되는가?
- [ ] 타입이 명확하게 정의되어 있는가?
- [ ] 재사용 가능한 로직이 공통 훅으로 분리되어 있는가?

#### 유틸리티 함수 리팩토링
- [ ] 함수가 순수 함수인가? (부작용 없음)
- [ ] 함수가 단일 책임을 가지는가?
- [ ] 타입이 명확하게 정의되어 있는가?
- [ ] JSDoc 주석이 충분한가?
- [ ] 에러 처리가 적절한가?

#### 상태 관리 리팩토링
- [ ] 상태가 적절히 분리되어 있는가?
- [ ] 불필요한 리렌더링이 발생하지 않는가?
- [ ] 상태 업데이트 로직이 명확한가?
- [ ] 타입이 명확하게 정의되어 있는가?

---

### 8.3 성능 최적화 리팩토링

#### 컴포넌트 최적화
- [ ] `React.memo`를 적절히 사용하는가?
- [ ] `useMemo`, `useCallback`을 적절히 사용하는가?
- [ ] 불필요한 리렌더링이 없는가?
- [ ] 코드 스플리팅이 적용되어 있는가?

#### 번들 크기 최적화
- [ ] 동적 import를 사용하는가?
- [ ] 불필요한 라이브러리가 없는가?
- [ ] 트리 쉐이킹이 제대로 작동하는가?

---

### 8.4 타입 안전성 강화

- [ ] 모든 함수/변수에 타입이 정의되어 있는가?
- [ ] `any` 타입 사용을 최소화하는가?
- [ ] 타입 가드를 적절히 사용하는가?
- [ ] API 응답 타입이 정의되어 있는가?

---

### 8.5 접근성 개선

- [ ] ARIA 레이블이 적절히 사용되는가?
- [ ] 키보드 네비게이션이 가능한가?
- [ ] 색상 대비가 충분한가?
- [ ] 스크린 리더 호환성을 고려하는가?

---

## 9. 참고 사항

- 모든 변경사항은 기존 기능에 영향을 주지 않아야 함
- 타입 안전성 유지
- 접근성 고려
- 성능 테스트 권장
- 레퍼런스 프로젝트의 검증된 패턴 활용

---

---

## 6. 레퍼런스 프로젝트 패턴

> **Reference**: PaysByPays Dashboard (`D:\dev\dashboard`)

레퍼런스 프로젝트에서 확인한 최신 패턴 및 베스트 프랙티스를 정리한 섹션입니다.

### 6.1 폼 컴포넌트 패턴

#### 폼 컴포넌트 분리 패턴

레퍼런스 프로젝트에서는 폼을 다음과 같이 분리합니다:

```
MerchantForm/
├── MerchantFormFields.tsx    # 폼 필드만 담당
├── MerchantFormActions.tsx    # 버튼 및 액션만 담당
└── MerchantFormSuccess.tsx   # 성공 상태 표시
```

**장점**:
- 관심사 분리 (필드 vs 액션)
- 재사용성 향상
- 테스트 용이성

**적용 예시**:
```tsx
// IssueFormFields.tsx
export function IssueFormFields({
  formData,
  errors,
  onChange,
}: IssueFormFieldsProps) {
  return (
    <div className="space-y-6">
      {/* 필드들 */}
    </div>
  );
}

// IssueFormActions.tsx
export function IssueFormActions({
  isSubmitting,
  submitError,
  onCancel,
}: IssueFormActionsProps) {
  return (
    <>
      {submitError && <ErrorDisplay />}
      <div className="flex gap-3 pt-4">
        <button onClick={onCancel}>취소</button>
        <button type="submit" disabled={isSubmitting}>
          {isSubmitting ? "저장 중..." : "저장"}
        </button>
      </div>
    </>
  );
}
```

### 6.2 Drawer vs Modal 패턴

레퍼런스 프로젝트에서는 **상세 보기**에는 `Drawer`를, **확인 다이얼로그**에는 `Modal`을 사용합니다.

**Drawer 사용 케이스**:
- 상세 정보 표시
- 긴 폼 (필드가 많은 경우)
- 사이드에서 슬라이드 인

**Modal 사용 케이스**:
- 확인 다이얼로그
- 짧은 폼 (필드가 적은 경우)
- 중앙에 표시

### 6.3 폼 상태 관리 패턴

레퍼런스 프로젝트에서는 폼 상태를 컴포넌트 내부에서 관리합니다:

```tsx
const [formData, setFormData] = useState<FormData>({
  field1: "",
  field2: "",
});

const [errors, setErrors] = useState<FormErrors>({});

const handleChange = (field: keyof FormData, value: string) => {
  setFormData((prev) => ({ ...prev, [field]: value }));
  // 에러 초기화
  if (errors[field]) {
    setErrors((prev) => ({ ...prev, [field]: undefined }));
  }
};
```

### 6.4 에러 처리 패턴

**필드별 에러 표시**:
```tsx
{errors.fieldName && (
  <p className="mt-1 text-sm text-red-600 dark:text-red-400">
    {errors.fieldName}
  </p>
)}
```

**전체 폼 에러 표시**:
```tsx
{submitError && (
  <div className="rounded-lg border border-red-200 bg-red-50 p-4 dark:border-red-800 dark:bg-red-900/20">
    <p className="text-sm text-red-800 dark:text-red-200">
      {submitError}
    </p>
  </div>
)}
```

### 6.5 로딩 상태 패턴

**스켈레톤 로딩**:
```tsx
{loading ? (
  <div className="space-y-4">
    {[1, 2].map((i) => (
      <div key={i} className="micro-card p-6">
        <div className="h-5 w-24 mb-4 bg-[var(--surface-muted)] rounded animate-pulse" />
        {/* 스켈레톤 내용 */}
      </div>
    ))}
  </div>
) : (
  // 실제 내용
)}
```

### 6.6 적용 권장사항

#### ✅ 적용 권장
1. **폼 컴포넌트 분리** (FormFields, FormActions) - 이미 적용됨
2. **Drawer 사용** (상세 보기, 긴 폼) - 이미 적용됨
3. **에러 처리 패턴** (필드별 + 전체) - 이미 적용됨
4. **로딩 상태 패턴** (스켈레톤) - 필요시 적용

#### ⚠️ 선택적 적용
1. **micro-* 클래스** - `@hua-labs/ui` 컴포넌트 사용 시 불필요
2. **커스텀 Select** - `@hua-labs/ui`의 `Select` 사용

**참고 파일**:
- `D:\dev\dashboard\src\components\merchants\MerchantFormFields.tsx` - 폼 필드 패턴
- `D:\dev\dashboard\src\components\merchants\MerchantFormActions.tsx` - 액션 패턴
- `D:\dev\dashboard\src\components\common\DetailDrawer.tsx` - Drawer 패턴

---

**문서 작성일**: 2025-11-29  
**마지막 업데이트**: 2025-11-29

