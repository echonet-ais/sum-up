# ============================================
# SumUp 환경 변수 설정
# ============================================
# 이 파일을 복사하여 .env.local 파일을 생성하고 실제 값을 입력하세요.
# .env.local 파일은 Git에 커밋하지 마세요.

# ============================================
# 필수 환경 변수 (반드시 설정 필요)
# ============================================

# API 설정
NEXT_PUBLIC_API_BASE_URL=https://api.example.com

# Supabase 설정 (Supabase 사용 시 필수)
# Supabase 프로젝트 대시보드에서 확인 가능
# https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# JWT 토큰 설정 (토큰 서명용, 강력한 랜덤 문자열 사용)
# Supabase 사용 시에도 추가 인증용으로 사용 가능
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# 이메일 발송 설정 (SendGrid, AWS SES, SMTP 중 하나 선택)
EMAIL_PROVIDER=sendgrid
SENDGRID_API_KEY=your-sendgrid-api-key
SENDGRID_FROM_EMAIL=noreply@example.com

# AI 기능 설정 (OpenAI, Anthropic, Google 중 하나 선택)
AI_PROVIDER=openai
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o-mini

# 앱 URL (이메일 링크용)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# 선택 환경 변수 (아래 설명 참조)
# ============================================

# JWT 토큰 만료 시간 (초 단위, 기본값: 24시간 = 86400초)
# JWT_EXPIRES_IN=86400

# SendGrid 발신자 이름
# SENDGRID_FROM_NAME=SumUp

# AWS SES 설정 (EMAIL_PROVIDER=ses인 경우)
# AWS_SES_REGION=us-east-1
# AWS_SES_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SES_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS_SES_FROM_EMAIL=noreply@example.com

# SMTP 설정 (EMAIL_PROVIDER=smtp 또는 nodemailer인 경우)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-email-password
# SMTP_FROM_EMAIL=noreply@example.com
# SMTP_FROM_NAME=SumUp

# Anthropic Claude 설정 (AI_PROVIDER=anthropic인 경우)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Google Gemini 설정 (AI_PROVIDER=google인 경우)
# GOOGLE_AI_API_KEY=your-google-ai-api-key
# GOOGLE_AI_MODEL=gemini-pro

# AI Rate Limiting 설정
# AI_RATE_LIMIT_PER_MINUTE=10
# AI_RATE_LIMIT_PER_DAY=100

# 데이터베이스 설정 (Supabase 사용 시 불필요, 목 데이터 사용 시 선택)
# DATABASE_TYPE=sqlite
# DATABASE_URL=./data/sumup.db

# PostgreSQL 설정 (DATABASE_TYPE=postgres인 경우, Supabase 사용 시 불필요)
# POSTGRES_URL=postgresql://user:password@localhost:5432/sumup

# MySQL 설정 (DATABASE_TYPE=mysql인 경우)
# MYSQL_URL=mysql://user:password@localhost:3306/sumup

# 파일 저장 설정 (선택, 기본값: 로컬)
# FILE_STORAGE_TYPE=local
# FILE_UPLOAD_DIR=./uploads

# AWS S3 설정 (FILE_STORAGE_TYPE=s3인 경우)
# AWS_S3_BUCKET=your-s3-bucket-name
# AWS_S3_REGION=us-east-1
# AWS_S3_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_S3_SECRET_ACCESS_KEY=your-aws-secret-access-key

# 환경 설정
# NODE_ENV=development

# 보안 설정
# COOKIE_SECRET=your-cookie-secret-key-change-this-in-production
# PASSWORD_RESET_TOKEN_EXPIRES_IN=3600
# TEAM_INVITE_TOKEN_EXPIRES_IN=604800

# 로깅 설정
# LOG_LEVEL=info

# CORS 설정
# CORS_ORIGIN=http://localhost:3000,https://sumup.vercel.app

# ============================================
# 환경 변수 설명
# ============================================
#
# [필수 변수]
#
# 1. NEXT_PUBLIC_API_BASE_URL
#    - 백엔드 API Base URL
#    - 목 데이터 사용 시: http://localhost:3000/api
#    - Supabase 사용 시: Supabase 프로젝트 URL 사용
#    - 예: https://api.example.com
#
# 2. Supabase 설정 (Supabase 사용 시 필수)
#    - NEXT_PUBLIC_SUPABASE_URL: Supabase 프로젝트 URL
#      예: https://xxxxx.supabase.co
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY: 공개 API 키 (클라이언트에서 사용)
#      Row Level Security (RLS) 정책으로 보호됨
#    - SUPABASE_SERVICE_ROLE_KEY: 서비스 역할 키 (서버에서만 사용)
#      RLS 정책을 우회하므로 절대 클라이언트에 노출하지 마세요
#    - Supabase 대시보드: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
#    - 자세한 설정 방법: docs/SUPABASE_SETUP_GUIDE.md 참조
#
# 3. JWT_SECRET
#    - JWT 토큰 서명용 시크릿 키
#    - Supabase 사용 시에도 추가 인증용으로 사용 가능
#    - 프로덕션에서는 강력한 랜덤 문자열 사용 필수
#    - 생성 방법: openssl rand -base64 32
#
# 4. 이메일 발송 (EMAIL_PROVIDER 선택)
#    - sendgrid: SendGrid API 사용
#    - ses: AWS SES 사용
#    - smtp: 일반 SMTP 서버 사용
#    - nodemailer: Nodemailer 사용
#
# 5. AI 기능 (AI_PROVIDER 선택)
#    - openai: OpenAI GPT 모델 사용
#    - anthropic: Anthropic Claude 모델 사용
#    - google: Google Gemini 모델 사용
#
# 6. NEXT_PUBLIC_APP_URL
#    - 앱의 공개 URL (이메일 링크 생성용)
#    - 개발: http://localhost:3000
#    - 프로덕션: https://sumup.vercel.app
#
# [선택 변수]
#
# - JWT_EXPIRES_IN: JWT 토큰 만료 시간 (기본값: 86400초 = 24시간)
# - AI_RATE_LIMIT_PER_MINUTE: AI API 분당 요청 제한 (기본값: 10)
# - AI_RATE_LIMIT_PER_DAY: AI API 일당 요청 제한 (기본값: 100)
# - DATABASE_TYPE: 데이터베이스 타입 (Supabase 사용 시 불필요)
# - FILE_STORAGE_TYPE: 파일 저장소 타입 (기본값: local)
# - NODE_ENV: 실행 환경 (development, production, test)
#
# [주의사항]
#
# 1. NEXT_PUBLIC_ 접두사가 붙은 변수는 클라이언트 측에서 접근 가능합니다.
# 2. 민감한 정보(API 키, 비밀번호 등)는 NEXT_PUBLIC_ 접두사를 사용하지 마세요.
#    - SUPABASE_SERVICE_ROLE_KEY는 절대 NEXT_PUBLIC_ 접두사를 사용하지 마세요.
# 3. 프로덕션 환경에서는 강력한 랜덤 문자열을 사용하세요.
# 4. 환경 변수 변경 후 개발 서버를 재시작하세요.
# 5. .env.local 파일은 절대 Git에 커밋하지 마세요.
# 6. Supabase 사용 시:
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY는 클라이언트에서 사용 가능하지만 RLS 정책으로 보호됩니다.
#    - SUPABASE_SERVICE_ROLE_KEY는 서버 사이드에서만 사용하고 절대 노출하지 마세요.
